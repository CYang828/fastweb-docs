language: node_js
node_js:
  - "lts/*"
branches:
  only:
  - master
cache:
  directories:
  - node_modules
  yarn: true
before_install:
- export TZ='Asia/Shanghai'
install:
- yarn
script:
- yarn docs:build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 74b5cae883d41910b5f3c7e5285c2ac0f430ea4b
  local-dir: ./sources/.vuepress/dist/
  target-branch: gh-pages
  verbose: true
  on:
    branch: master